# Smile2Unlock Dependencies
# Python 3.11.9 required
# 
# IMPORTANT: Install dlib via conda FIRST:
#   conda install "dlib=20.0.0=*cpu*" -c conda-forge -y
#
# Then install these packages:
#   pip install -r requirements.txt

cmake==3.29.3
face_recognition==1.3.0
opencv-contrib-python==4.9.0.80
pynput==1.8.1
screeninfo==0.8.1
numpy==1.26.4
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# Build tools (only needed for packaging)
pyinstaller==6.16.0
